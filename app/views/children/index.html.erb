<h1>Listing children</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Parentname</th>
    <%# <th>Age</th>
    <th>Parentcompany</th>
    <th>Emailaddr</th>
    <th>Phone</th>
    <th>Status</th>
    <th>Remarks</th> %>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @children.each do |child| %>
  <tr>
    <td><%= child.name %></td>
    <td><%= child.parentname %></td>
    <%# <td><%= child.age %></td>
    <%# <td><%= child.parentcompany %></td>
    <%# <td><%= child.emailaddr %></td>
    <%# <td><%= child.phone %></td>
    <%# <td><%= child.status %></td>
    <%# <td><%= child.remarks %></td>
    <td><%= link_to 'Show', child %></td>
    <td><%= link_to 'Edit', edit_child_path(child) %></td>
    <td><%= link_to 'Destroy', child, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Child', new_child_path %>
